apiVersion: 1

providers:
  - name: 'Tessera Dashboards'
    orgId: 1
    folder: 'Tessera'
    folderUid: 'tessera'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
